# Smart Inbox Assistant

An AI first-pass triage system for a pharmacovigilance shared mailbox. It reads
incoming email and PDF attachments, works out what each message is about,
extracts the key facts with a source reference for every one, and hands the
result to a human reviewer to accept or override — so a reviewer checks the
AI's work instead of starting from a blank form.

Built for the **Clinevo Technologies — Forward Deployment / GenAI Integration
Engineer** live project assignment.

> **No real patient data is used anywhere in this project.** Every document in
> the corpus is synthetic and generated by [`pkg/generator`](pkg/generator).

---

## Status

Phases 0–2a complete: the corpus generator and the full ingestion +
preprocessing pipeline run offline with 33 passing tests. Next is the model
layer (OCR, classification, field extraction). See
[Build plan](#build-plan).

## The problem

A shared mailbox receives email and PDF attachments from doctors, patients and
other sources every day. Someone reads every one by hand, decides what kind of
message it is, and pulls out the important details. It is slow and error-prone,
and it is the *first pass* — not the clinical judgement — that is automatable.

Each message is sorted into one or more of four buckets:

| Category | Plain meaning | Signals |
|---|---|---|
| **Safety Report** (ICSR) | A patient had a bad reaction to a drug | An identifiable patient, a reporter, a drug, and an adverse outcome — all four, even loosely |
| **Quality Complaint** (PQC) | Something is physically wrong with the product | Broken seal, wrong colour, contamination, damaged packaging, counterfeit |
| **Info Request** (MI) | A question about a product | Dosing, administration, interactions — with no reaction and no defect |
| **Not Relevant** | Everything else | Marketing, spam, internal admin |

Labels are **multi-label, not exclusive** — a bad reaction caused by a
defective product is legitimately both an ICSR and a PQC, and the system is
built to say so rather than forced to pick one.

## Architecture

```
Angular  ──▶  Django + DRF  ──▶  Python AI service  ──▶  Oracle
(reviewer UI)  (REST, orchestration,   (LLM / OCR /        (messages,
                queue, persistence)     extraction)         extractions,
                                        FastAPI, pkg/       audit log)
```

Processing is queued rather than synchronous: OCR and LLM calls take seconds to
a minute per document, so the API accepts work and returns immediately while
workers drain the queue.

| Layer | Technology | Why |
|---|---|---|
| Frontend | Angular | As specified — reviewer queue, document viewer, editable fields |
| Backend API | **Django + DRF (Python)** | REST, orchestration, queue, persistence — *see deviation note below* |
| AI services | Python (FastAPI), in `pkg/` | LLM and OCR work, a separate service behind REST |
| Database | Oracle (PL/SQL) | As specified — messages, extracted records, audit log |
| AI model | Gemini (vision-capable) | Scanned/handwritten pages and image description need vision; cheap with a usable free tier |

### Deviation from the suggested stack: Django instead of Spring Boot

The assignment suggests Spring Boot for the backend and permits deviation with
justification. This project uses **Django + Django REST Framework**, for three
reasons:

1. **It is the team's production expertise.** The assignment ends in a live
   walkthrough where the build has to be explained and defended under
   questioning. A backend built in a less familiar stack would be weaker in
   exactly the place the evaluation looks hardest.
2. **The AI tier is Python regardless.** Extraction, OCR and LLM orchestration
   have no Java equivalent worth the friction — which is the assignment's own
   stated example of a good reason to deviate. A Python backend shares typing,
   tooling and test infrastructure with the AI service instead of straddling
   two ecosystems for one prototype.
3. **The architecture the assignment actually asks for is preserved.** The
   point of the suggested stack is *separation of concerns*, not Java itself.
   The AI service remains a **separate process behind its own REST API**, not
   library calls inside the web app, so the four-tier boundary the brief
   describes is intact — only the language of the middle tier changed.

Everything else follows the suggested stack: Angular frontend, Oracle with real
PL/SQL, a queue rather than synchronous calls.

Tech choices and their trade-offs are argued in full in the
[write-up](docs/) once written.

## Ingestion pipeline

```
.eml ──▶ parse MIME ──▶ per attachment ──▶ detect flavour ──▶ extract ──▶ normalise
          headers,        PDF or not?        digital        text blocks    prose only
          body, parts                        scanned        tables         (tables and
                                             article        images         images are
                                             non-English    metadata        untouched)
```

**Flavour detection is evidence-based, not filename-based.** Real mail arrives
as `scan0001.pdf` and `document.pdf`, so the signal comes from the page: a
missing text layer plus a full-page image means scanned; stopword frequency
identifies the language; structural markers (`abstract`, `references`,
`discussion`) identify an article. Verified 14/14 against ground truth.

**Tables are extracted as tables.** Rows and columns are recovered from the
PDF's own cell geometry and kept as header + rows, then rendered to the model
as Markdown. Flattening a lab panel into prose would destroy the link between a
value, its unit and its reference range — which is the only thing that makes
the value meaningful.

**Preprocessing never touches tables or images.** Normalisation collapses
whitespace, and whitespace inside a cell is load-bearing (`3.5 - 5.0`, `20 mg`).
Prose gets Unicode normalisation, de-hyphenation, dot-leader and page-number
stripping, and page-furniture removal; structured content is passed through
untouched. There is a test asserting this holds across the entire corpus.

**Non-PDF attachments are logged, not dropped.** They still produce a record
with `processed=False` and a warning, so a reviewer can see something arrived
that the pipeline chose not to read.

## Two design commitments

These shape the data model rather than sitting on top of it, so they are
decided now rather than retrofitted:

**1. Every extracted fact carries its source.** A field is never stored as a
bare value — it is stored with the document and page it came from, so a
reviewer can always answer "where did this come from?". This is an audit
requirement, not a nicety.

**2. "Unknown" beats a guess.** Any field not stated in the source is recorded
as `Not stated`, never inferred, and every field carries a confidence score. A
confident wrong answer is worse than an honest gap in this domain.

## Setup

Prerequisites: **Node 20+**, **Python 3.12**, and **Docker Desktop running**.
No JDK is required — see the deviation note above.

Each tier is self-contained and installs independently, so the three stacks
never share state and can be built or run in any order.

```bash
cp .env.example .env          # then fill in mailbox + GEMINI_API_KEY
docker compose up -d oracle   # database

cd pkg      && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
cd frontend && npm install
cd backend  && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
```

Full per-service run instructions land as each phase completes.

## Build plan

- [x] **Phase 0** — Repo scaffold, `.env.example`, Oracle compose file
- [x] **Phase 1** — Synthetic corpus generator + ground-truth labels
- [x] **Phase 2a** — Ingestion: mail parsing, PDF flavour detection, tables, metadata
- [ ] **Phase 2b** — Model layer: OCR/vision, classification, field extraction
- [ ] **Phase 3** — Oracle schema + PL/SQL audit package
- [ ] **Phase 4** — Django: IMAP poller, queue, REST API
- [ ] **Phase 5** — Angular reviewer screen
- [ ] **Phase 6** — Batch run, timings, accuracy against ground truth
- [ ] **Phase 7** — Bonus: literature screening
- [ ] **Phase 8** — Write-up, architecture diagram, screen recording

## Repository layout

```
frontend/          Angular reviewer UI          -- npm, self-contained
backend/           Django + DRF REST API        -- own venv, self-contained
pkg/               Python: all AI features      -- own venv, self-contained
  ai_service/        FastAPI service: extraction, classification, OCR
  generator/         Synthetic corpus generator
  tests/             Unit tests
db/ddl/            Oracle schema DDL
db/plsql/          PL/SQL packages (audit log, stats)
data/samples/      Generated corpus (gitignored) + committed ground truth
docs/              Write-up and architecture diagram
```
